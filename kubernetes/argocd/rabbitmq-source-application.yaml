apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: rabbitmq-source
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/ais25131/cloud-platforms.git
    targetRevision: main
    path: kubernetes/apps/rabbitmq-source/manifests
  destination:
    server: https://kubernetes.default.svc
    namespace: knative-sources
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
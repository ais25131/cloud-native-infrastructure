apiVersion: serving.knative.dev/v1beta1
kind: DomainMapping
metadata:
  name: node-red.local
  namespace: node-red
spec:
  ref:
    name: node-red-knative
    kind: Service
    apiVersion: serving.knative.dev/v1
---
- name: Update apt cache
  apt:
    update_cache: true

- name: Install common packages
  apt:
    name:
      - curl
      - wget
      - git
      - vim
      - net-tools
      - unzip
      - ca-certificates
      - gnupg
      - lsb-release
    state: present